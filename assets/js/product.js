document.addEventListener("DOMContentLoaded",()=>{
 const q=new URLSearchParams(location.search),packageId=q.get("package"),p=packageId?KD_PACKAGES.find(x=>x.id===packageId):KD_PRODUCTS.find(x=>x.id===(q.get("id")||"majica"));if(!p)return;
 const isPack=!!packageId,packProducts=isPack?p.items.map(name=>KD_PRODUCTS.find(x=>x.name===name)).filter(Boolean):[],image=isPack?(packProducts.find(x=>x.image)?.image||""):p.image;
 const colors=isPack?["Crna","Bela","Plava","Crvena"]:p.colors,sizes=isPack?["S","M","L","XL","XXL"]:p.sizes,positions=isPack?["Prema proizvodu"]:p.positions;
 document.title=`${p.name} | Kumovi Debili`;
 const gallery=isPack?`<div class="package-gallery">${packProducts.map(x=>`<article>${x.image?`<img src="${x.image}" alt="${esc(x.name)}">`:`<div class="scribble">Fotografija uskoro</div>`}<strong>${esc(x.name)}</strong></article>`).join("")}</div>`:(image?`<img id="product-preview-image" src="${image}" alt="${esc(p.name)}">`:`<div class="scribble">Fotografija uskoro</div>`);
 $("#product-view").innerHTML=`<div class="product-layout"><div><div class="product-main-image ${isPack?"package-stage":""}">${gallery}</div>${!isPack&&colors.length>1?`<div id="preview-color-label" class="preview-color-label">Pregled boje: ${esc(colors[0])}</div>`:""}</div><div class="product-info"><span class="eyebrow">${isPack?"Paket":"Personalizacija"}</span><h1>${esc(p.name)}</h1><p class="lead">${esc(isPack?p.items.join(" · "):p.desc)}</p><div class="meta">${isPack?"Paket":esc(p.category)}</div><div class="price">TRI PRASETA GROK</div><div class="notice">Nakon slanja upita potvrđujemo cenu, dostupnost, rok i način dostave.</div><form id="personalize" class="form-box"><div class="form-grid"><div class="field"><label for="color">${isPack?"Boja majica":"Boja"}</label><select id="color">${colors.map(x=>`<option>${esc(x)}</option>`).join("")}</select></div><div class="field"><label for="qty">${isPack?"Broj članova ekipe":"Količina"}</label><input id="qty" type="number" min="1" max="500" value="1" required></div>${sizes.length?`<div class="field full"><label>Raspodela majica po veličinama</label><div class="size-grid">${sizes.map(s=>`<label>${s}<input data-size="${s}" type="number" min="0" value="0"></label>`).join("")}</div><small id="size-help">${isPack?"Zbir veličina mora biti jednak broju članova ekipe.":""}</small></div>`:""}<div class="field"><label for="printText">Tekst za štampu</label><input id="printText" maxlength="200" placeholder="Npr. KUM — glavni krivac"></div><div class="field"><label for="position">Pozicija štampe</label><select id="position">${positions.map(x=>`<option>${esc(x)}</option>`).join("")}</select></div><div class="field full"><label for="note">Dodatna napomena</label><textarea id="note" maxlength="1000" placeholder="Boje i detalji za ostale proizvode iz paketa..."></textarea></div><div class="field full upload"><label for="design">Dodaj sliku ili logo</label><input id="design" type="file" accept=".jpg,.jpeg,.png,.pdf,.svg,image/jpeg,image/png,application/pdf,image/svg+xml"><small>JPG, JPEG, PNG, PDF ili SVG · najviše 20 MB</small><div id="file-info"></div><img id="file-preview" class="preview hidden" alt="Pregled dizajna"><button id="remove-file" class="btn alt hidden" type="button">Ukloni fajl</button></div><label class="checkbox field full"><input id="applyAll" type="checkbox"><span>Primeni isti dizajn na sve proizvode iz porudžbine</span></label><div id="form-msg" class="field full"></div><button class="btn lime field full" type="submit">Dodaj u korpu</button></div></form></div></div>`;
 const shirtImages={"Crna":"assets/images/majica-crna.png","Bela":"assets/images/majica-bela-transparent.png","Plava":"assets/images/majica-plava.png","Crvena":"assets/images/majica-crvena.png"};
 const colorFilters={"Crna":"grayscale(1) brightness(.45) contrast(1.2)","Bela":"grayscale(1) brightness(1.2)","Plava":"sepia(1) saturate(5) hue-rotate(165deg) brightness(.72)","Crvena":"sepia(1) saturate(8) hue-rotate(300deg) brightness(.7)","Zelena":"sepia(1) saturate(5) hue-rotate(70deg) brightness(.75)","Bež":"sepia(.7) saturate(.8) brightness(1.08)","Siva":"grayscale(1) brightness(.8)","Žuta":"sepia(1) saturate(5) brightness(1.05)","Narandžasta":"sepia(1) saturate(7) hue-rotate(330deg) brightness(.9)","Maslinasta":"sepia(1) saturate(3) hue-rotate(35deg) brightness(.65)","Transparentna":"none"};
 const color=$("#color"),productImage=$("#product-preview-image"),label=$("#preview-color-label");
 function updateColor(){if(!productImage)return;const selected=color.value;if(p.id==="majica")productImage.src=shirtImages[selected]||shirtImages.Bela;else productImage.style.filter=colorFilters[selected]||"none";if(label)label.textContent=`Pregled boje: ${selected}`}
 color.addEventListener("change",updateColor);updateColor();
 const input=$("#design"),preview=$("#file-preview"),info=$("#file-info"),remove=$("#remove-file");let fileMeta=null;
 function clearFile(){input.value="";preview.src="";preview.classList.add("hidden");remove.classList.add("hidden");info.textContent="";fileMeta=null}
 input.addEventListener("change",()=>{const file=input.files[0];if(!file)return;const allowed=["image/jpeg","image/png","application/pdf","image/svg+xml"];if(!allowed.includes(file.type)||file.size>20*1024*1024){clearFile();$("#form-msg").innerHTML=`<div class="error-msg">Fajl mora biti JPG, PNG, PDF ili SVG i manji od 20 MB.</div>`;return}fileMeta={name:file.name,type:file.type,size:file.size};info.textContent=`Izabrano: ${file.name}`;remove.classList.remove("hidden");if(file.type.startsWith("image/")&&file.type!=="image/svg+xml"){preview.src=URL.createObjectURL(file);preview.classList.remove("hidden")}});
 remove.addEventListener("click",clearFile);
 $("#personalize").addEventListener("submit",e=>{e.preventDefault();const sizeQty={};$$("[data-size]").forEach(i=>{if(+i.value)sizeQty[i.dataset.size]=+i.value});const qty=+$("#qty").value,sizeTotal=Object.values(sizeQty).reduce((a,b)=>a+b,0);if(sizes.length&&sizeTotal!==qty){$("#form-msg").innerHTML=`<div class="error-msg">Zbir veličina (${sizeTotal}) mora biti jednak količini (${qty}).</div>`;return}KDCart.add({type:isPack?"package":"product",id:p.id,name:p.name,image,qty,color:color.value,position:$("#position").value,printText:$("#printText").value.trim(),note:$("#note").value.trim(),sizeQty,file:fileMeta,applyAll:$("#applyAll").checked});$("#form-msg").innerHTML=`<div class="success-msg">Dodato u korpu. Ekipa može dalje.</div>`});
});
